onEvent('productivebees', function (event) {
/*
    // data/productivebees/productivebees/* (thermal, etc)
    data = {
        recipes: [
            {
                primaryColor: '#e9edf3',
                secondaryColor: '#bdccd9',
                particleColor: '#ffd86f',
                description: 'productivebees.ingredient.description.only_spawnegg',
                createComb: false,
                beeTexture: 'productivebees:textures/entity/bee/blitz/bee',
                flowerBlock: 'emendatusenigmatica:potassium_nitrate_block',
                weather_tolerance: 2,
                type: 'forge:mod_loaded',
                modid: 'thermal'
            }
        ]
    };

    data.recipes.forEach((recipe) => {

            event.custom({
                type: `productivebees:productivebees/${modid}`,
                primaryColor: recipe.primaryColor,
                secondaryColor: recipe.secondaryColor,
                particleColor: recipe.particleColor,
                description: recipe.description,
                createComb: recipe.createComb,
                beeTexture: recipe.beeTexture,
                flowerBlock: recipe.flowerBlock,
                attributes: {
                    weather_tolerance: recipe.weather_tolerance
                },
                conditions: [
                    {
                        type: recipe.type,
                        modid: recipe.modid
                    }
                ]

            });

    });
*/
});
